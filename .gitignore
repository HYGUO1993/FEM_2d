# Build output
dist/
build/pyi/

# CMake generated files
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
*.cmake

# cx_Freeze build output
build/exe*/
FEM_2d_Studio.egg-info/

# Compiled binaries (except the FEM solver we want to track)
*.o
*.a
*.so
*.exe
!build/bin/Release/fem_run.exe

# IDE / editor files
.vscode/
.idea/
*.user

# macOS
.DS_Store

# Python cache
__pycache__/
*.pyc
*.pyo

# Runtime output artifacts
Results*.dat
ElemStiff.dat

# Conda env
.env

# ===== FemLab Studio Tauri 前端 (gui_tauri) =====
# Rust 构建产物 — 内含运行时配置(可能含 API key 的 debug exe), 严禁上传
gui_tauri/src-tauri/target/
# 前端依赖与构建
gui_tauri/node_modules/
gui_tauri/dist/
# 临时扫描/调试脚本(本地上传前检查用)
scan_secrets.py
# 所有 Rust 构建目录变体 (target, target_dbg, target_dbg2, ...)
gui_tauri/src-tauri/target*/
# 构建日志与中间产物
build/*.log
build/*.txt
build/obj/
# 工具运行时数据
.omo/
.playwright-mcp/
启动GUI_venv.bat
gui_tauri/target*/
# 本地归档(不随 GitHub 同步)
_archive_local/
build_ci_test/
build/x64/
